services:
  rungu:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/codecoradev/rungu:latest
    container_name: rungu
    restart: unless-stopped
    ports:
      - "${RUNGU_PORT:-3000}:3000"
    volumes:
      - rungu-data:/data
    environment:
      - RUST_LOG=${RUST_LOG:-rungu=info}
      - APP_SECRET=${APP_SECRET:?APP_SECRET is required. Generate with: openssl rand -hex 32}
      - APP_URL=${APP_URL:-http://localhost:3000}
      # OAuth providers (leave empty to disable)
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID:-}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET:-}
      - KEYCLOAK_URL=${KEYCLOAK_URL:-}
      - KEYCLOAK_REALM=${KEYCLOAK_REALM:-}
      - KEYCLOAK_CLIENT_ID=${KEYCLOAK_CLIENT_ID:-}
      - KEYCLOAK_CLIENT_SECRET=${KEYCLOAK_CLIENT_SECRET:-}
    command: >
      serve
      --db /data/rungu.db
      --listen 0.0.0.0:3000
    healthcheck:
      test: ["CMD", "/rungu", "healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  rungu-data:
